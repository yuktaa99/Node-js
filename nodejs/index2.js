const greet = (n) => {
  console.log(`Hello ${n}`);
  console.log("Hello," + n);
};

greet("Yukta");
greet("Jai");

const add = (x, y) => x + y;
//   console.log(`${x + y}`);

console.log(add(1, 2));

// console.log(`Fifteen is ${a + b} and
// not ${2 * a + b}.`);
// "Fifteen is 15 and
// not 20."
